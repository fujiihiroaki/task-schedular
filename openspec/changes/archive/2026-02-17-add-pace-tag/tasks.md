## 1. パーサーの拡張

- [x] 1.1 `MarkdownTaskParser.cs` の `case "pace":` にバリデーション・正規化ロジックを追加する（`TryNormalizePace` メソッドの実装）
- [x] 1.2 不正なペース値の検出時に `Console.Error` へ警告メッセージを出力する処理を追加する
- [x] 1.3 同一タスク内で `pace` が重複した場合に最後の有効値を採用するロジックを実装する

## 2. 推論ロジックの更新

- [x] 2.1 `Inference.cs` の `_PaceToLead()` メソッドを拡張し、既定ペースレベルの日数を更新する（nonstop=1, rapid=7, fast=14, normal=30, slow=90）
- [x] 2.2 `_PaceToLead()` は正規化済み `name` / `name=days` の変換のみに責務を限定する（`pace:` 接頭辞の解釈・検証は `MarkdownTaskParser` 側で実施）

## 3. レンダリングの更新

- [x] 3.1 `MarkdownRenderer.cs` のペースタグ出力が正規化された形式（小文字、カスタム日数保持）で出力されることを確認する
- [x] 3.2 `MarkdownRenderer.cs` のメタデータガイドにペースタグの説明を更新する（新ペースレベル・カスタム日数形式の追記）

## 4. テストの追加

- [x] 4.1 既定ペースレベル（nonstop, rapid, fast, normal, slow）のパースと日数変換のテストを追加する
- [x] 4.2 カスタム日数（`pace:fast=60`, `pace:custom=45`）のパースと日数変換のテストを追加する
- [x] 4.3 不正値（未知名、範囲外日数、非整数）のバリデーションと警告出力のテストを追加する
- [x] 4.4 大文字小文字の正規化テストを追加する
- [x] 4.5 同一タスク内のペース重複テストを追加する
- [x] 4.6 start / lead / pace の優先順位テストを追加する
- [x] 4.7 レンダリングの正規化出力テストを追加する
- [x] 4.8 カスタム形式の未知名（`pace:unknown=45`）が有効として扱われるテストを追加する
- [x] 4.9 name 文字種不正（`pace:bad!name=30`）の検出と無視のテストを追加する
- [x] 4.10 警告メッセージに不正値・理由・識別情報（`id` または `title`）が含まれるテストを追加する
- [x] 4.11 整数正規化（`pace:FAST=007` → `pace:fast=7`）のパースおよびレンダリングテストを追加する

## 5. ドキュメントの更新

- [x] 5.1 README.md のペースタグ日数表を新しい既定値に更新し、nonstop / rapid を追記する
- [x] 5.2 README.md に `pace:<name>=<days>` 形式とバリデーション範囲（1〜2048）の説明を追記する
